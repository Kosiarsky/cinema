# Frontend SSR environment
# Copy to .env and fill values. Used only during SSR (Node). For CSR use src/assets/runtime-config.js.

API_BASE_URL=http://localhost:8000
FRONTEND_BASE_URL=http://localhost:4200
PORT=4000
